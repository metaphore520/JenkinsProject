pipeline {
    agent { docker { 
    
    image 'node:16.17.1-alpine' 
    label 'docker-slave'
    
    } }
    stages {
        stage('build') {
            steps {
                sh 'node --version'
            }
        }
    }
}
